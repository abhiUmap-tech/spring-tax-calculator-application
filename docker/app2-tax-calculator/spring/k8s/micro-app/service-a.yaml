apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a
  labels:
    app: service-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      name: service-a
      labels:
        app: service-a
    spec:
      containers:
        - name: service-a
          image: embarkx/tax-service-a
          env:
            - name: TAX_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  key: TAX_SERVICE_URL
                  name: tax-service-config
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP

      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: service-a-svc
spec:
  selector:
    app: service-a
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
